# راهنمای نقش‌ها و شاخص‌های ارزیابی (Team Roles & KPIs)

سیستم ارزیابی از ۸ نقش کارگروهی پشتیبانی می‌کند. هر نقش ۶ شاخص عددی (امتیاز ۱ تا ۱۰)
و ۴ سؤال تشریحی دارد. همه تعاریف در یک فایل نگهداری می‌شوند:

> `src/lib/roles.ts` — تنها منبع حقیقت برای نقش‌ها، شاخص‌ها و ماتریس دسترسی.

افزودن یک نقش جدید = یک مقدار در enum `WorkgroupRole` (فایل `prisma/schema.prisma`)
به‌علاوه یک ورودی در `ROLE_DEFINITIONS` و `EVALUATION_MATRIX`. هیچ تغییر دیگری در
دیتابیس، API یا فرم‌ها لازم نیست.

---

## ۱. نقش‌ها

| کلید (DB) | عنوان فارسی |
|---|---|
| `STRATEGIST` | استراتژیست |
| `STRATEGIST_ASSISTANT` | دستیار استراتژیست |
| `WRITER` | نویسنده |
| `DESIGNER` | گرافیست |
| `EDITOR` | تدوینگر |
| `VIDEOGRAPHER` | تصویربردار |
| `SOCIAL_ADMIN` | ادمین |
| `ONSITE_ADMIN` | ادمین حضوری |

## ۲. ماتریس دسترسی ارزیابی

هر ردیف نشان می‌دهد که آن نقش اجازه ارزیابی کدام نقش‌ها را دارد.

| ارزیاب | می‌تواند ارزیابی کند |
|---|---|
| استراتژیست | همه نقش‌ها (شامل نقش خودش) |
| دستیار استراتژیست | همه نقش‌ها (شامل نقش خودش) |
| نویسنده | همه نقش‌ها (شامل نقش خودش) |
| گرافیست | همه نقش‌ها (شامل نقش خودش) |
| تدوینگر | استراتژیست، دستیار استراتژیست، تدوینگر، گرافیست، تصویربردار |
| تصویربردار | استراتژیست، دستیار استراتژیست، تصویربردار، گرافیست، تدوینگر |
| ادمین | استراتژیست، دستیار استراتژیست، نویسنده، گرافیست، ادمین |
| ادمین حضوری | استراتژیست، دستیار استراتژیست، نویسنده، تصویربردار، تدوینگر، گرافیست، ادمین حضوری |

این ماتریس در صفحه **مدیریت ← نقش‌ها و دسترسی‌ها** به‌صورت زنده نمایش داده می‌شود.

**«نقش خودش» به معنای ارزیابی هم‌نقش‌ها است، نه ارزیابی خود فرد.** خودارزیابی به‌صورت
پیش‌فرض غیرفعال است؛ برای فعال کردن آن `ALLOW_SELF_EVALUATION` را در `src/lib/roles.ts`
برابر `true` قرار دهید.

## ۳. شاخص‌های هر نقش (۱ تا ۱۰)

**استراتژیست:** استراتژی محتوا · نتیجه کار · بریف و توضیحات · پیگیری و اصلاح ·
همکاری با تیم · خلاقیت و ایده

**دستیار استراتژیست:** پیگیری کارها · درک استراتژی و نیاز پروژه · ارتباط و همکاری ·
انتقال شفاف به تیم · رصد و اصلاح عملکرد محتوا · خلاقیت و ایده

**نویسنده:** کیفیت محتوا · درک بریف · خلاقیت در نوشتن · دقت و ویرایش ·
تعهد به زمان‌بندی · مشارکت و ارتباط در جلسات

**گرافیست:** درک بریف · خلاقیت و ایده‌پردازی · میزان اصلاحات · تعهد به زمان‌بندی ·
همکاری با تیم · مشارکت و ارتباط در جلسات

**تدوینگر:** درک بریف · دقت در بررسی فایل‌های خام · میزان اصلاحات ·
تعهد به زمان‌بندی · خلاقیت در تدوین · همکاری با تیم

**تصویربردار:** درک بریف · کیفیت فیلمبرداری · تسلط در اجرای پروژه ·
تعهد به زمان‌بندی · خلاقیت در فیلمبرداری · همکاری با تیم

**ادمین:** دقت در انتشار محتوا · نظم در مدیریت پیج · سرعت پاسخگویی ·
کیفیت پاسخگویی · تشخیص موارد مهم · همکاری با تیم

**ادمین حضوری:** آمادگی برای تولید محتوا · دقت در ثبت محتوا · درک نیاز محتوا ·
مدیریت زمان در روز تولید · سرعت عمل و حل مسئله · همکاری با تیم

### نکته درباره «میزان اصلاحات»

این شاخص (گرافیست و تدوینگر) در متن اصلی معکوس است — اصلاحات بیشتر یعنی عملکرد
ضعیف‌تر. برای اینکه میانگین امتیازها معنادار بماند، جهت شاخص در فرم یکسان نگه داشته
شده و راهنمای «امتیاز بالاتر یعنی اصلاحات کمتر» زیر آن نمایش داده می‌شود.

## ۴. سؤالات تشریحی

هر نقش ۴ سؤال دارد که در دیتابیس در این ستون‌ها ذخیره می‌شوند:
`strengths` (نقطه قوت) · `improvements` (نقطه قابل بهبود) · `example` (یک مثال واقعی) ·
`suggestions` (پیشنهاد). متن سؤال‌ها برای هر نقش متفاوت است و از `ROLE_DEFINITIONS`
خوانده می‌شود.

---

## ۵. ساختار فنی

| بخش | مسیر |
|---|---|
| تعریف نقش‌ها، شاخص‌ها و ماتریس | `src/lib/roles.ts` |
| محاسبه افراد/نقش‌های قابل ارزیابی | `src/lib/team-evaluations.ts` |
| مدل دیتابیس | `RoleEvaluation` در `prisma/schema.prisma` |
| API ثبت و خواندن | `src/app/api/evaluations/role/route.ts` |
| فرم ارزیابی | `src/components/evaluations/role-evaluation-form.tsx` |
| لیست ارزیابی‌ها | `src/components/evaluations/role-evaluations-table.tsx` |
| صفحات | `src/app/(dashboard)/evaluations/team/` |
| مدیریت نقش‌ها | `src/app/(dashboard)/admin/roles/` |

امتیازها در ستون JSON `scores` ذخیره می‌شوند (`{ metricKey: 1..10 }`) به‌همراه
`totalScore` و `averageScore` برای گزارش‌گیری سریع. یادداشت‌های اختیاری هر شاخص در
`metricNotes` نگهداری می‌شوند.

**قانون یکتایی:** هر ارزیاب برای هر فرد، در هر نقش، کارگروه، ماه و سال فقط یک ارزیابی
می‌تواند ثبت کند.

---

## ۶. اجرا

```bash
# ۱. تولید Prisma Client
npx prisma generate

# ۲. اعمال مایگریشن روی دیتابیس
npx prisma migrate deploy

# ۳. بیلد
npm run build
```

مایگریشن: `prisma/migrations/20260826120000_add_team_roles_and_role_evaluations`

این مایگریشن enum `WorkgroupRole` را بازسازی می‌کند (شش نقش جدید)، مقدار
`ROLE_EVALUATION` را به `CommentType` اضافه می‌کند و جدول `RoleEvaluation` را می‌سازد.
داده‌های موجود `STRATEGIST` و `WRITER` دست‌نخورده باقی می‌مانند.

---

## ۷. تخصیص نقش در داشبورد

دو مسیر وجود دارد:

1. **مدیریت ← نقش‌ها و دسترسی‌ها** → دکمه «تخصیص نقش» روبه‌روی هر کاربر →
   انتخاب کارگروه و نقش. برای حذف، روی ✕ کنار برچسب نقش کلیک کنید.
2. **مدیریت ← کارگروه‌ها** → «مدیریت اعضا» → افزودن کاربر با نقش دلخواه.

یک کاربر می‌تواند هم‌زمان چند نقش و عضویت در چند کارگروه داشته باشد؛ دسترسی ارزیابی
او اجتماع دسترسی‌های همه نقش‌هایش است.

پس از تخصیص نقش، کاربر در منو گزینه **ارزیابی تیم** را می‌بیند و می‌تواند فقط
نقش‌ها و افرادی را ارزیابی کند که ماتریس اجازه می‌دهد.
