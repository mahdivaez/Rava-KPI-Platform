# 📋 راهنمای نقش‌های چندگانه (Multi-Role System)

## ✅ سیستم نقش‌های چندگانه فعال است!

یک کاربر می‌تواند **همزمان** در کارگروه‌های مختلف نقش‌های متفاوت داشته باشد.

---

## 🎯 مثال واقعی

### کاربر: احمد محمدی

**کارگروه A**: استراتژیست  
**کارگروه B**: نویسنده  
**کارگروه C**: استراتژیست

### منوی احمد محمدی:
```
📊 داشبورد

استراتژیست
  ✍️ ارزیابی نویسنده‌ها

نویسنده
  💬 ارسال بازخورد
```

احمد می‌تواند:
- ✅ نویسنده‌های کارگروه A و C را ارزیابی کند
- ✅ بازخورد به استراتژیست کارگروه B ارسال کند
- ✅ **همه اینها در یک پنل**

---

## 🔧 نحوه تنظیم نقش‌های چندگانه

### قدم 1: ایجاد کاربر
از **مدیریت > کاربران** یک کاربر جدید ایجاد کنید.

### قدم 2: افزودن به کارگروه اول
1. برو به **مدیریت > کارگروه‌ها**
2. روی آیکون "Users" کلیک کن
3. کاربر را انتخاب کن
4. نقش: **استراتژیست** انتخاب کن
5. افزودن عضو

### قدم 3: افزودن به کارگروه دوم
1. برو به کارگروه دیگری
2. روی آیکون "Users" کلیک کن
3. **همان کاربر** را انتخاب کن
4. نقش: **نویسنده** انتخاب کن
5. افزودن عضو

### نتیجه
✅ این کاربر حالا **هر دو منو** را در sidebar خود می‌بیند!

---

## 📊 سناریوهای مختلف

### سناریو 1: فقط استراتژیست
```
کاربر در 2 کارگروه = استراتژیست

منوی کاربر:
  استراتژیست
    ✍️ ارزیابی نویسنده‌ها
```

### سناریو 2: فقط نویسنده
```
کاربر در 3 کارگروه = نویسنده

منوی کاربر:
  نویسنده
    💬 ارسال بازخورد
```

### سناریو 3: هر دو نقش (Multi-Role)
```
کاربر:
  - کارگروه A: استراتژیست
  - کارگروه B: نویسنده
  - کارگروه C: استراتژیست

منوی کاربر:
  استراتژیست
    ✍️ ارزیابی نویسنده‌ها
  نویسنده
    💬 ارسال بازخورد
```

---

## 🔍 نحوه کار صفحات

### صفحه ارزیابی نویسنده‌ها
هنگامی که استراتژیست این صفحه را باز می‌کند:
- فقط **کارگروه‌هایی که در آن استراتژیست است** نمایش داده می‌شود
- فقط **نویسنده‌های آن کارگروه‌ها** قابل ارزیابی هستند

### صفحه ارسال بازخورد
هنگامی که نویسنده این صفحه را باز می‌کند:
- فقط **کارگروه‌هایی که در آن نویسنده است** نمایش داده می‌شود
- فقط **استراتژیست‌های آن کارگروه‌ها** قابل ارسال بازخورد هستند

---

## 🎯 تست Multi-Role

### برای تست این قابلیت:

#### 1. ایجاد 2 کارگروه
```
کارگروه A: تولید محتوا
کارگروه B: مارکتینگ
```

#### 2. ایجاد یک کاربر تست
```
نام: علی احمدی
ایمیل: ali@test.com
رمز: 123456
```

#### 3. افزودن به کارگروه A به عنوان استراتژیست
```
کارگروه: تولید محتوا
کاربر: علی احمدی
نقش: استراتژیست
```

#### 4. افزودن به کارگروه B به عنوان نویسنده
```
کارگروه: مارکتینگ
کاربر: علی احمدی
نقش: نویسنده
```

#### 5. ورود به حساب علی احمدی
```
ali@test.com / 123456
```

#### 6. بررسی Sidebar
شما باید **هر دو بخش** را ببینید:
- ✅ استراتژیست → ارزیابی نویسنده‌ها
- ✅ نویسنده → ارسال بازخورد

---

## 🚨 نکات مهم

### ✅ چیزهایی که می‌شود
- یک نفر در کارگروه A استراتژیست و در B نویسنده باشد ✅
- یک نفر در چند کارگروه استراتژیست باشد ✅
- یک نفر در چند کارگروه نویسنده باشد ✅
- هر دو منو در یک sidebar نمایش داده شود ✅

### ❌ چیزهایی که نمی‌شود
- یک نفر نمی‌تواند ارزیابی خودش را ببیند ❌
- نویسنده نمی‌تواند ارزیابی‌های خودش را ببیند ❌
- استراتژیست نمی‌تواند بازخوردهای درباره خودش را ببیند ❌

---

## 💡 نمونه کد

### چگونه سیستم تشخیص می‌دهد؟

```typescript
// در Sidebar
const memberships = await prisma.workgroupMember.findMany({
  where: { userId: session.user.id },
})

const isStrategist = memberships.some(m => m.role === 'STRATEGIST')
const isWriter = memberships.some(m => m.role === 'WRITER')

// اگر isStrategist === true → منوی استراتژیست نمایش داده می‌شود
// اگر isWriter === true → منوی نویسنده نمایش داده می‌شود
// اگر هر دو true باشند → هر دو منو نمایش داده می‌شوند
```

---

## 📸 مثال تصویری

### داشبورد کاربر با نقش چندگانه:

```
┌─────────────────────────────────────┐
│ سیستم KPI                           │
├─────────────────────────────────────┤
│ 📊 داشبورد                         │
│                                     │
│ مدیریت (اگر Admin باشد)            │
│   👥 کاربران                       │
│   📁 کارگروه‌ها                    │
│   📊 گزارشات                        │
│                                     │
│ استراتژیست                         │
│   ✍️ ارزیابی نویسنده‌ها           │
│                                     │
│ نویسنده                            │
│   💬 ارسال بازخورد                 │
└─────────────────────────────────────┘
```

---

## 🎓 آموزش برای Admin

### چگونه یک کاربر Multi-Role ایجاد کنیم؟

1. **کاربر را یک بار ایجاد کنید**
   - مدیریت > کاربران > کاربر جدید

2. **به هر کارگروه که نیاز دارید اضافه کنید**
   - مدیریت > کارگروه‌ها
   - آیکون Users
   - همان کاربر + نقش مورد نظر

3. **تمام!**
   - کاربر حالا می‌تواند با یک حساب کاربری
   - در کارگروه‌های مختلف
   - نقش‌های مختلف داشته باشد
   - و همه را در یک پنل ببیند

---

## 🔗 ارتباط با معاون فنی

### تفاوت معاون فنی با Multi-Role:

```
معاون فنی:
  - فقط یک نفر در کل سیستم
  - ارزیابی استراتژیست‌ها
  - دسترسی به بازخوردها
  - نقش سیستمی (در User مشخص می‌شود)

استراتژیست/نویسنده:
  - تعداد نامحدود
  - بستگی به کارگروه دارد
  - نقش کارگروهی (در WorkgroupMember مشخص می‌شود)
  - می‌تواند در کارگروه‌های مختلف متفاوت باشد
```

---

**✨ سیستم نقش‌های چندگانه به طور کامل پیاده‌سازی شده است! ✨**

