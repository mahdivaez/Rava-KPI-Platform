# 🚀 راهنمای نصب و راه‌اندازی سریع

## قدم اول: بررسی پیش‌نیازها ✅

مطمئن شوید موارد زیر نصب شده‌اند:
- Node.js 18+ 
- PostgreSQL 14+

## قدم دوم: نصب Dependencies 📦

```bash
npm install
```

## قدم سوم: تنظیم دیتابیس PostgreSQL 🗄️

### نصب PostgreSQL (اگر نصب نیست)

**macOS (با Homebrew):**
```bash
brew install postgresql@16
brew services start postgresql@16
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

**Windows:**
دانلود و نصب از: https://www.postgresql.org/download/windows/

### ایجاد دیتابیس

```bash
# ورود به PostgreSQL
psql -U postgres

# ایجاد دیتابیس جدید
CREATE DATABASE kpi_platform;

# ایجاد کاربر (اختیاری)
CREATE USER kpi_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE kpi_platform TO kpi_user;

# خروج
\q
```

## قدم چهارم: تنظیم فایل .env 🔧

فایل `.env` در ریشه پروژه ایجاد شده است. آن را ویرایش کنید:

```bash
nano .env
# یا
code .env
```

مقادیر زیر را بر اساس تنظیمات خود تغییر دهید:

```env
# آدرس دیتابیس خود را وارد کنید
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/kpi_platform"

# یک کلید امن تولید کنید
AUTH_SECRET="your-secret-key-here"

NEXTAUTH_URL="http://localhost:3000"
```

### تولید AUTH_SECRET

```bash
openssl rand -base64 32
```

خروجی را کپی کرده و در `.env` قرار دهید.

## قدم پنجم: اجرای Migration 🔄

```bash
# ایجاد جداول دیتابیس
npx prisma migrate dev --name init

# تولید Prisma Client (اگر خودکار انجام نشد)
npx prisma generate
```

## قدم ششم: ایجاد کاربر مدیر اولیه 👤

```bash
npx prisma db seed
```

این دستور یک کاربر مدیر با مشخصات زیر ایجاد می‌کند:
- **ایمیل**: `admin@kpi.com`
- **رمز عبور**: `Admin@123`

## قدم هفتم: اجرای پروژه 🎉

```bash
# محیط توسعه (Development)
npm run dev
```

پروژه روی آدرس زیر اجرا می‌شود:
**http://localhost:3000**

## ورود به سیستم 🔐

1. به آدرس http://localhost:3000 بروید
2. با اطلاعات زیر وارد شوید:
   - ایمیل: `admin@kpi.com`
   - رمز عبور: `Admin@123`

⚠️ **مهم**: پس از اولین ورود، حتماً رمز عبور را تغییر دهید!

## مراحل بعدی 📝

### 1. ایجاد کاربران
از منوی **مدیریت > کاربران** می‌توانید:
- کاربران جدید ایجاد کنید
- نقش‌های مختلف اختصاص دهید (مدیر، معاون فنی، استراتژیست، نویسنده)
- وضعیت کاربران را مدیریت کنید

### 2. ایجاد کارگروه‌ها
از منوی **مدیریت > کارگروه‌ها** می‌توانید:
- کارگروه‌های جدید ایجاد کنید
- اعضا را به کارگروه‌ها اضافه کنید
- نقش هر عضو را مشخص کنید (استراتژیست یا نویسنده)

### 3. شروع ارزیابی‌ها
- **معاون فنی**: می‌تواند استراتژیست‌ها را ارزیابی کند
- **استراتژیست**: می‌تواند نویسنده‌های کارگروه خود را ارزیابی کند
- **نویسنده**: می‌تواند بازخورد به استراتژیست‌ها ارسال کند

## دستورات مفید 🛠️

```bash
# مشاهده دیتابیس در Prisma Studio
npx prisma studio

# Reset کردن دیتابیس (حذف همه داده‌ها)
npx prisma migrate reset

# Build برای production
npm run build
npm start
```

## عیب‌یابی 🐛

### خطا: Cannot connect to database

**راه حل:**
1. مطمئن شوید PostgreSQL در حال اجرا است:
   ```bash
   # macOS
   brew services list
   
   # Linux
   sudo systemctl status postgresql
   ```

2. `DATABASE_URL` در `.env` را بررسی کنید
3. تست اتصال:
   ```bash
   psql -U postgres -d kpi_platform
   ```

### خطا: AUTH_SECRET is not defined

**راه حل:**
1. مطمئن شوید فایل `.env` در ریشه پروژه وجود دارد
2. `AUTH_SECRET` را تولید و تنظیم کنید:
   ```bash
   openssl rand -base64 32
   ```

### خطا: Prisma Client not generated

**راه حل:**
```bash
npx prisma generate
```

### پورت 3000 در حال استفاده است

**راه حل:**
```bash
# اجرا روی پورت دیگر
PORT=3001 npm run dev
```

## نیاز به کمک؟ 💬

اگر مشکلی داشتید:
1. فایل `README.md` را مطالعه کنید
2. لاگ‌های خطا را بررسی کنید
3. Issue در مخزن ایجاد کنید

---

✨ **موفق باشید!**

