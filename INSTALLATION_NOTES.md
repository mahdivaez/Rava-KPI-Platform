# 🎉 پروژه با موفقیت ایجاد شد!

## ✅ وضعیت نصب

**همه چیز آماده است!** پروژه کامل ایجاد شده و منتظر راه‌اندازی شماست.

---

## 🚨 مراحل بعدی (مهم!)

### قبل از اجرای پروژه، باید:

#### 1. PostgreSQL را نصب کنید
اگر PostgreSQL نصب نیست:

**macOS:**
```bash
brew install postgresql@16
brew services start postgresql@16
```

**Linux:**
```bash
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

**Windows:**
از سایت رسمی دانلود کنید: https://www.postgresql.org/download/

#### 2. دیتابیس ایجاد کنید
```bash
# وارد PostgreSQL شوید
psql -U postgres

# دیتابیس ایجاد کنید
CREATE DATABASE kpi_platform;

# خارج شوید
\q
```

#### 3. فایل .env را تنظیم کنید

فایل `.env` در ریشه پروژه وجود دارد. آن را **ویرایش** کنید:

```bash
# باز کردن فایل
nano .env
# یا
code .env
```

**مقادیر زیر را تغییر دهید:**

```env
# آدرس دیتابیس خود را وارد کنید
# username و password را با اطلاعات خود جایگزین کنید
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/kpi_platform"

# یک کلید امن تولید کنید
AUTH_SECRET="$(openssl rand -base64 32)"

NEXTAUTH_URL="http://localhost:3000"
```

**برای تولید AUTH_SECRET:**
```bash
openssl rand -base64 32
```

خروجی را کپی کرده و در `.env` قرار دهید.

#### 4. Migration اجرا کنید
```bash
npx prisma migrate dev --name init
```

این دستور:
- جداول دیتابیس را ایجاد می‌کند
- Prisma Client را generate می‌کند

#### 5. Seed اجرا کنید (کاربر admin)
```bash
npx prisma db seed
```

این دستور کاربر مدیر اولیه را ایجاد می‌کند:
- ایمیل: `admin@kpi.com`
- رمز: `Admin@123`

#### 6. پروژه را اجرا کنید
```bash
npm run dev
```

---

## 🌐 دسترسی به پروژه

پس از اجرا، به آدرس زیر بروید:

**http://localhost:3000**

با اطلاعات زیر وارد شوید:
- **ایمیل**: admin@kpi.com
- **رمز عبور**: Admin@123

⚠️ **حتماً** پس از اولین ورود، رمز عبور را تغییر دهید!

---

## 📚 مستندات

برای اطلاعات بیشتر:

1. **QUICKSTART.md** → شروع سریع در 5 دقیقه
2. **SETUP.md** → راهنمای کامل نصب
3. **README.md** → مستندات جامع پروژه
4. **PROJECT_SUMMARY.md** → خلاصه پروژه و ویژگی‌ها

---

## 🎯 چک‌لیست راه‌اندازی

- [ ] PostgreSQL نصب شد
- [ ] دیتابیس `kpi_platform` ایجاد شد
- [ ] فایل `.env` تنظیم شد
- [ ] `DATABASE_URL` درست وارد شد
- [ ] `AUTH_SECRET` تولید و وارد شد
- [ ] `npx prisma migrate dev` اجرا شد
- [ ] `npx prisma db seed` اجرا شد
- [ ] `npm run dev` اجرا شد
- [ ] http://localhost:3000 باز شد
- [ ] با admin وارد شدم

---

## ❓ مشکل دارید؟

### خطای اتصال به دیتابیس

**راه‌حل:**
1. مطمئن شوید PostgreSQL در حال اجراست:
   ```bash
   # macOS
   brew services list
   
   # Linux
   sudo systemctl status postgresql
   ```

2. `DATABASE_URL` را در `.env` بررسی کنید
3. username و password را چک کنید

### خطای AUTH_SECRET

**راه‌حل:**
```bash
# تولید کلید جدید
openssl rand -base64 32

# آن را در .env قرار دهید
```

### پورت 3000 اشغال است

**راه‌حل:**
```bash
# اجرا روی پورت دیگر
PORT=3001 npm run dev
```

### خطاهای دیگر

1. `node_modules` را حذف کنید و دوباره نصب کنید:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

2. Prisma Client را دوباره generate کنید:
   ```bash
   npx prisma generate
   ```

---

## 🛠️ دستورات مفید

```bash
# مشاهده دیتابیس در مرورگر
npx prisma studio

# Reset کامل دیتابیس
npx prisma migrate reset

# مشاهده لاگ‌های Prisma
DATABASE_URL="..." npx prisma db pull

# Build برای production
npm run build
npm start
```

---

## 📁 ساختار پروژه

```
rava-platform/
├── prisma/              # Schema و seed دیتابیس
├── src/
│   ├── app/            # صفحات Next.js
│   ├── components/     # کامپوننت‌های React
│   ├── lib/            # توابع کمکی
│   └── types/          # TypeScript types
├── README.md           # مستندات کامل
├── SETUP.md            # راهنمای نصب
├── QUICKSTART.md       # شروع سریع
└── PROJECT_SUMMARY.md  # خلاصه پروژه
```

---

## 🎨 ویژگی‌های پیاده‌سازی شده

✅ احراز هویت کامل (Login/Logout)
✅ مدیریت کاربران (CRUD)
✅ مدیریت کارگروه‌ها
✅ ارزیابی استراتژیست‌ها
✅ ارزیابی نویسنده‌ها
✅ سیستم بازخورد
✅ گزارشات و داشبورد
✅ رابط کاربری فارسی (RTL)
✅ طراحی مدرن و responsive
✅ کنترل دسترسی دقیق

---

## 🚀 بعد از راه‌اندازی

### کاربران جدید ایجاد کنید
از منوی **"مدیریت > کاربران"** کاربران جدید با نقش‌های مختلف ایجاد کنید.

### کارگروه‌ها بسازید
از منوی **"مدیریت > کارگروه‌ها"** کارگروه‌های خود را ایجاد کنید.

### اعضا اضافه کنید
روی آیکون "Users" در لیست کارگروه‌ها کلیک کنید و اعضا را با نقش مورد نظر (استراتژیست یا نویسنده) اضافه کنید.

### شروع ارزیابی
از منوهای مربوطه شروع به ارزیابی و ارسال بازخورد کنید.

---

## 💡 نکات مهم

1. **امنیت**: حتماً `AUTH_SECRET` قوی تولید کنید
2. **دیتابیس**: backup منظم از دیتابیس بگیرید
3. **Production**: در محیط production از HTTPS استفاده کنید
4. **رمز عبور**: رمز admin را فوراً تغییر دهید
5. **Environment**: هرگز `.env` را commit نکنید

---

**✨ همه چیز آماده است! موفق باشید! ✨**

در صورت نیاز به کمک، مستندات را مطالعه کنید یا Issue ایجاد کنید.

